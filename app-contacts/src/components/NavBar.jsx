const NavBar = () => {
  return (
    <nav className="navbar">
      <div className="container-fluid d-flex justify-content-center">
        <h1>CRUD FullStack con React, Python, Django (DRF) y MySQL</h1>
      </div>
    </nav>
  );
};

export default NavBar;
